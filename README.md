# IPTV Manager для OpenWrt v3.16

Скрипт для настройки IPTV на роутере OpenWrt с веб-админ панелью, встроенным HLS-плеером, EPG телепрограммой и расписанием обновлений.

## Возможности

- **LuCI-плагин** — полная интеграция в веб-интерфейс OpenWrt (Services → IPTV Manager)
- **Управление сервером из LuCI** — запуск, остановка и мониторинг сервера прямо из вкладки «Сервер»
- **Системная информация** — аптайм, оперативная память, свободное место на диске прямо в админке
- **Первоначальная настройка** — пошаговый визард (плейлист, EPG, расписание, автозапуск)
- **Сервер без плейлиста** — запускается сразу, настройка через админку
- Загрузка плейлиста по ссылке (M3U/M3U8), из файла или от провайдера
- **Название плейлиста** — произвольное имя для отображения в статистике
- **Валидация плейлиста** — проверка URL перед загрузкой (доступность + количество каналов)
- **Объединение плейлистов** — загрузка нескольких плейлистов по URL в один файл
- **Авто-обнаружение EPG** — автоматически находит ссылку EPG из заголовка плейлиста (`url-tvg=`)
- **Гибридный EPG** — хранится в RAM (/tmp) в формате gz (~15MB), не занимает флеш-память
- **EPG телепрограмма** — загрузка и раздача XMLTV, авто-распаковка gzip, авто-повтор при ошибке (3 попытки)
- **Проверка каналов** — проверка доступности потоков прямо из админки
- **Избранное** — отмечайте любимые каналы и быстро фильтруйте их
- **Встроенный HLS-плеер** — воспроизведение каналов прямо в браузере с боковым списком каналов, поиском и EPG
- **PiP (Картинка в картинке)** — смотрите канал в маленьком окне и продолжайте работу в браузере
- **3 темы** — светлая, тёмная и OpenWrt Bootstrap Dark (🟣). Авто-определение при встраивании в LuCI
- **Загрузка плейлистов в плеере** — поддержка любых M3U/M3U8 плейлистов
- **Расписание обновлений** — автообновление плейлиста и EPG
- **Безопасность** — пароль на админку (HTTP Basic Auth), API токен, белый список IP, rate limiting
- **Проверка обновлений** — автоматическая проверка новой версии с GitHub
- Веб-админ панель в стиле OpenWrt/Keenetic
- Бэкап и восстановление настроек
- Автозапуск при старте роутера
- Полное удаление одной командой
- Авто-обновление скрипта из GitHub при каждом запуске

## Установка

```sh
wget -q -O - https://raw.githubusercontent.com/whatuneeed/IPTV-Manager/main/IPTV-Manager.sh | sh
```

> Скрипт автоматически обновляется до последней версии с GitHub при каждом запуске.

После установки: `iptv`

## Веб-админ панель

| Страница | URL |
|----------|-----|
| **Админ-панель** | `http://192.168.1.1:8082/cgi-bin/admin.cgi` |
| **Плейлист** | `http://192.168.1.1:8082/playlist.m3u` |
| **EPG** | `http://192.168.1.1:8082/epg.xml` |
| **HLS-плеер** | `http://192.168.1.1:8082/player.html` |

### Вкладки админки

**Каналы** — список каналов с фильтрацией по группам, поиском, проверкой доступности и кнопкой «Смотреть всё»

**Плейлист** — загрузка по ссылке, просмотр исходного M3U, обновление

**Телепрограмма** — настройка EPG, просмотр передач, поддержка XML и XML.gz

**Настройки** — расписание автообновления плейлиста и EPG, бэкап и восстановление, проверка обновлений, пароль, API токен, белый список IP, валидация и объединение плейлистов

## HLS-плеер

Откройте `http://192.168.1.1:8082/player.html` для просмотра каналов прямо в браузере.

### Возможности плеера

- 📺 **Список каналов** — боковая панель с логотипами, группами и поиском
- 📡 **«Сейчас играет»** — текущая передача из EPG отображается в списке каналов
- 🔍 **Поиск** — мгновенная фильтрация по названию канала
- 📂 **Фильтр по группам** — выбор группы из выпадающего списка
- 📋 **Загрузка плейлистов** — вкладка «Плейлист» для загрузки любого M3U/M3U8 по URL
- ⌨️ **Горячие клавиши**:
  - `↑` / `↓` — переключение каналов
  - `Пробел` — пауза / воспроизведение
  - `F` — полный экран
- 🎬 **Полноэкранный режим** — кнопка ⛶ или клавиша F
- 📱 **Адаптивный дизайн** — боковая панель скрывается на мобильных устройствах

### URL-параметры плеера

| Параметр | Описание | Пример |
|----------|----------|--------|
| `url` | Прямая ссылка на поток | `player.html?url=http://...` |
| `idx` | Индекс канала для автостарта | `player.html?url=...&idx=5` |
| `pl` | URL плейлиста для загрузки | `player.html?pl=http://...m3u` |

## Подключение телевизора

В IPTV плеере (TiviMate, IPTV Smarters, VLC, SS IPTV):

- **Плейлист:** `http://192.168.1.1:8082/playlist.m3u`
- **EPG:** `http://192.168.1.1:8082/epg.xml`

## Расписание обновлений

Планировщик работает в фоне и проверяет каждую минуту, пришло ли время обновить:

| Интервал | Когда обновляет |
|----------|-----------------|
| Каждый час | Каждые 60 минут |
| Каждые 6ч | Каждые 6 часов |
| Каждые 12ч | Два раза в сутки |
| Раз в сутки | Раз в 24 часа |

После обновления плейлист и EPG автоматически копируются в `/www/iptv/` — телевизор получает актуальные данные без перезапуска сервера.

## SSH меню

```
── Плейлист ──────────────────────────
 1) Загрузить по ссылке
 2) Загрузить из файла
 3) Настроить провайдера
 4) Обновить плейлист

── Телепрограмма ─────────────────────
 5) Настроить EPG
 6) Обновить EPG
 7) Удалить EPG

── Сервер ────────────────────────────
 8) Запустить
 9) Остановить

── Настройки ─────────────────────────
10) Расписание обновлений
11) Автозапуск

── Удаление ──────────────────────────
12) Удалить плейлист
13) Удалить IPTV Manager

Enter) Выход
```

## LuCI-плагин (интеграция в OpenWrt)

Плагин `luci-app-iptv-manager` добавляет IPTV Manager в веб-интерфейс OpenWrt как нативный раздел **Services → IPTV Manager**.

### Вкладки плагина

| Вкладка | Описание |
|---------|----------|
| **IPTV** | iframe: полная админка (таблица каналов, проверка, редактирование, плейлист, EPG, настройки) |
| **Плеер** | iframe: HLS-плеер с каналами, полноэкранный режим, PiP |
| **Сервер** | Управление IPTV сервером — запуск, остановка, мониторинг статуса. Работает даже при остановленном сервере (через CGI на порту 80) |

### Управление сервером из LuCI

Вкладка **Сервер** позволяет запускать и останавливать IPTV сервер прямо из LuCI без доступа к терминалу:

- **Статус** — показывает «● Запущен» или «○ Остановлен»
- **Запустить** — запускает uhttpd на порту 8082, генерирует CGI и копирует файлы
- **Остановить** — останавливает uhttpd на порту 8082
- Фоновое CGI (`srv.cgi` на порту 80) гарантирует работу вкладки даже когда основной сервер остановлен

### Установка плагина

**Через скрипт установки** — при первоначальной настройке выберите «Установить LuCI-плагин».

**Вручную:**

```sh
# Скопируйте файлы на роутер:
scp -r luci-app-iptv-manager/htdocs/luci-static/resources/view/iptv-manager/* /www/luci-static/resources/view/iptv-manager/
scp -r luci-app-iptv-manager/root/usr/share/luci/menu.d/ /usr/share/luci/menu.d/
scp -r luci-app-iptv-manager/root/usr/share/rpcd/acl.d/ /usr/share/rpcd/acl.d/
scp -r luci-app-iptv-manager/root/etc/uci-defaults/ /etc/uci-defaults/
/etc/init.d/rpcd restart
```

### Структура плагина

```
luci-app-iptv-manager/
├── Makefile                          # Сборка OpenWrt package
├── htdocs/luci-static/resources/view/iptv-manager/
│   ├── iptv.js                       # Вкладка IPTV (iframe админки)
│   ├── player.js                     # Вкладка Плеер (iframe)
│   ├── server.js                     # Вкладка Сервер (iframe + fallback)
│   ├── srv.html                      # HTML-интерфейс управления сервером
│   └── srv.cgi                       # CGI для старта/стопа сервера (порт 80)
├── root/
│   ├── etc/uci-defaults/99-luci-iptv-manager   # Инициализация
│   └── usr/share/
│       ├── luci/menu.d/                          # LuCI меню (OpenWrt 24+)
│       └── rpcd/acl.d/                           # ACL права доступа
└── luasrc/
    ├── controller/iptv-manager.lua   # Контроллер (OpenWrt 21-23)
    ├── model/cbi/iptv-manager/       # CBI-модели (OpenWrt 21-23)
    └── view/iptv-manager/            # Шаблоны (OpenWrt 21-23)
```

## Структура файлов

```
/etc/iptv/
├── iptv.conf           # Конфигурация плейлиста
├── playlist.m3u        # Текущий плейлист
├── provider.conf       # Данные провайдера
├── epg.conf            # Конфигурация EPG
├── schedule.conf       # Расписание обновлений
├── favorites.json      # Избранные каналы
├── security.conf       # Пароль и API токен
└── ip_whitelist.txt    # Белый список IP

/tmp/
├── iptv-epg.xml.gz     # EPG в RAM (сжатый, ~15MB)
└── iptv-merged.m3u     # Объединённый плейлист

/www/iptv/
├── cgi-bin/admin.cgi   # Веб-админка
├── player.html         # HLS-плеер
├── channels.json       # JSON-список каналов
├── playlist.m3u        # Плейлист для раздачи
└── server.html         # Интерфейс управления сервером

/www/cgi-bin/
├── srv.cgi             # CGI управления сервером (порт 80)
└── srv.html            # HTML управления сервером
```

## Популярные EPG источники

- `https://iptvx.one/EPG.XML` — Россия/СНГ (рекомендуется)
- `https://iptvx.one/EPG_NOARCH` — Россия/СНГ без архива
- `http://epg.it999.ru/epg.xml.gz` — Россия (сжатый)
- `http://epg.pockettv.net/epg.xml.gz` — Россия/СНГ

## Авто-обнаружение EPG

Если в плейлисте в первой строке указана ссылка на EPG (`url-tvg="..."`), скрипт автоматически:
1. Сохранит её как источник EPG
2. Скачает телепрограмму при загрузке плейлиста
3. Отобразит уведомление в админке

## Изменения в v3.16

- **LuCI Сервер v3** — `server.js` работает через `/cgi-bin/srv.cgi` на порту 80, без CORS проблем
- **srv.cgi** — управление сервером на порту 80 (работает всегда, даже без запущенного uhttpd на 8082)
- **Авто-перезагрузка** — после обновления/сброса страница сама проверяет готовность сервера и перезагружается
- **Проверка версий** — убрана md5sum проверка (ложные срабатывания из-за CRLF), только версионная проверка
- **Обновление с сохранением** — сначала скачивает, потом применяет, сервер запускается автоматически
- **Сброс к заводским** — скачивает свежий скрипт, сбрасывает настройки, автоматически запускает сервер
- **Авто-запуск сервера** — после обновления и сброса сервер поднимается сам

## Изменения в v3.15

- **LuCI Сервер v2** — исправленные кнопки запуска/остановки, корректное определение статуса
- **LuCI Сервер v1** — фикс `get_first` ошибки, убрана зависимость от `require uci`
- **Эмодзи тем** — исправлены: ☀️ Light, 🌙 Dark, 🟣 OpenWrt
- **Синхронизация тем** — `localStorage` синхронизирует тему между страницами
- **Кнопки обновления** — "Обновить с сохранением" и "Сброс к заводским" в настройках
- **Проверка обновлений** — теперь проверяет версию перед скачиванием

## Изменения в v3.14

- **Uptime сервера** — показывает время работы IPTV сервера в панели статистики
- **RAM** — использование оперативной памяти скриптом и его файлами
- **Диск** — использование свободного места файлами IPTV Manager
- **Регенерация channels.json** — автоматическое обновление после загрузки плейлиста
- **EPG таблица** — корректно работает даже без загруженного EPG

## Изменения в v3.13

- **Системная информация** — аптайм, RAM (использовано/всего), свободное место на диске в панели статистики
- **EPG авто-повтор** — при ошибке загрузки EPG автоматически пробует 3 раза с интервалом 5 сек
- **Валидация плейлиста** — проверка URL перед заменой (доступность + подсчёт каналов)
- **Объединение плейлистов** — загрузка нескольких плейлистов по URL и склейка в один файл
- **Регенерация channels.json** — автоматическое обновление после загрузки/обновления плейлиста
- **Rate limiting** — защита от DDoS (60 запросов/мин, блокировка на 5 мин)
- **Белый список IP** — ограничение доступа к админке по IP адресам
- **Исправлен EPG** — таблица телепрограммы корректно работает даже без загруженного EPG
- **Исправлен Unicode** — srv.html использует UTF-8 напрямую вместо \uXXXX
- **Исправлен grep -c** — убран баг `grep -c || echo 0` который возвращал `0\n0`
- **init.d скрипт** — переписан на procd для корректного управления демоном

## Изменения в v3.12

- **Вкладка «Сервер» в LuCI** — запуск, остановка и мониторинг сервера прямо из LuCI
- **srv.cgi + srv.html** — CGI на порту 80 для управления сервером, работает даже при остановленном uhttpd
- **Исправлен старт сервера** — CGI сначала отправляет ответ, потом перезапускает uhttpd в фоне
- **Исправлен stop_server** — убийство uhttpd в отдельном фоновом процессе с задержкой
- **Убраны зависимости от ubus/rpc.declare** — server.js больше не нуждается в модуле ubus
- **Исправлен Unicode** — srv.html вынесен в отдельный файл с нормальным UTF-8
- **Исправлен grep -c** — убран баг `grep -c || echo 0` который возвращал `0\n0` и ломал арифметику
- **CRLF авто-fix** — скрипт автоматически удаляет `\r` из файла при запуске
- **Авто-обновление** — скрипт проверяет GitHub и обновляет себя при запуске
- **LuCI плагин упрощён** — 3 вкладки (IPTV, Плеер, Сервер) вместо 6
- **Темы** — 3 темы: светлая, тёмная и OpenWrt Bootstrap Dark в админке и плеере
- **Фон плеера** — изменён с `#000` на `var(--bg)`, соответствует текущей теме
- **Full-screen** — плеер поддерживает полноэкранный режим при встраивании в LuCI iframe
- **uninstall** — теперь удаляет LuCI плагин, конфиги, ACL и init.d скрипт
- **Исправлен запуск** — сервер запускается даже без плейлиста

## Изменения в v3.11

- **EPG хранится только в gz** — `/tmp/iptv-epg.xml.gz` (~15MB RAM), без распаковки
- **Стриминг EPG** — `epg.cgi` отдаёт XML через `gunzip -c`, буфер ~32KB
- **Парсинг «Сейчас играет» из gz** — `gunzip -c | awk | head`, ~2MB RAM
- **LuCI-плагин** — полная интеграция в OpenWrt (Services → IPTV Manager)
- **CBI-формы** — нативные настройки плейлиста, EPG, расписания, безопасности
- **iframe-вкладки** — каналы и плеер встроены в LuCI

## Изменения в v3.10

- **Гибридный EPG** — хранится в RAM (`/tmp/iptv-epg.xml`), не занимает флеш-память
- **Исправлена распаковка .gz** — через временный файл, без ошибки `mv`
- **Динамическая таблица EPG** — загружается через AJAX (`get_epg`), кнопка «🔄 Обновить»
- **Название плейлиста** — поле во вкладке «Плейлист», отображается в статистике
- **Предупреждение о размере EPG** — если > 10MB, рекомендуется использовать EPG_LITE
- **Автообновление «Сейчас играет»** — после загрузки EPG через админку

## Изменения в v3.9

- **Первоначальная настройка** — пошаговый визард (5 шагов: файлы, плейлист, EPG, расписание, автозапуск)
- **Сервер без плейлиста** — запуск без плейлиста, настройка через админку
- **Подменю в SSH** — главное меню с подменю для каждой категории, кнопка «Назад»
- **Безопасность только в настройках** — секция пароля и API токена перенесена на вкладку «Настройки»

## Изменения в v3.8

- **Пароль на админку** — HTTP Basic Auth, настройка через админку
- **API токен** — защита CGI-эндпоинтов через заголовок `X-API-Token`
- **Проверка обновлений** — сравнение с GitHub прямо из админки и SSH-меню
- **Установка/переустановка** — пункты в SSH-меню для установки и переустановки с сохранением настроек

## Изменения в v3.7

- **Избранное в админке** — кнопка ★/☆ в каждой строке таблицы каналов
- **Фильтр «Избранное»** — быстрый показ только отмеченных каналов
- **Избранное в плеере** — отдельная вкладка «★ Избранное» в боковой панели
- **PiP (Картинка в картинке)** — кнопка ⧉ в панели плеера для просмотра в плавающем окне
- **Избранное сохраняется** — localStorage в плеере, favorites.json на сервере
- **Избранное в бэкапе** — включено в архив экспорта

## Изменения в v3.6

- **Встроенный HLS-плеер** — воспроизведение каналов с боковым списком, поиском и EPG
- **Загрузка плейлистов в плеере** — поддержка любых M3U/M3U8 плейлистов по URL
- **Кнопка «Смотреть всё»** — открытие плеера со списком каналов
- **Улучшенная проверка каналов** — корректная проверка HLS-потоков (m3u8)
- **Оптимизация производительности** — channels.json генерируется только при запуске сервера
- **Уменьшен размер скрипта** — объединены дублирующиеся awk-парсеры
- **Адаптивный дизайн** — поддержка мобильных устройств
- **Горячие клавиши** — стрелки, пробел, полный экран
- **Автозапуск сервера** — перегенерация CGI и channels.json при каждом запуске

## Изменения в v3.5

- Кнопка **▶ Смотреть** — открывает HLS-плеер с потоком канала
- Статистика плейлиста перемещена наверх вкладки «Каналы»
- Исправлены кавычки и спецсимволы в названиях каналов
- Кнопка выбора файла для импорта в стиле остальных кнопок

## Изменения в v3.4

- «Сейчас играет» — текущая передача из EPG в таблице каналов (сопоставление по tvg-id)
- Статистика плейлиста — группы, HD/SD каналы, размер
- Бэкап и восстановление — экспорт/импорт всех настроек

## Изменения в v3.3

- Исправлено отображение каналов — генерация через awk → файл → CGI подстановка
- Пагинация по 150 каналов на странице
- Логотипы каналов (tvg-logo) в таблице
- Поддержка HTTPS для загрузки плейлиста и EPG

## Изменения в v3.2

- «Сейчас играет» — текущая передача из EPG подставляется в таблицу каналов (сопоставление по tvg-id)
- Логотипы каналов (tvg-logo) отображаются в таблице
- Поддержка HTTPS для загрузки плейлиста (--no-check-certificate)
- Лимит каналов увеличен до 200

## Изменения в v3.1

- Переключатель светлой/тёмной темы (кнопка в шапке)
- CSS-переменные для всех цветов
- Сохранение выбранной темы в localStorage

## Изменения в v3.0

- Новый дизайн админки в стиле OpenWrt/Keenetic (светлая тема)
- Авто-обнаружение EPG из плейлиста
- Исправлена проверка каналов (ping)
- Поддержка HTTPS для EPG (`--no-check-certificate`)
- Авто-распаковка gzip по сигнатуре файла
- URL-декодирование ссылок из форм
- Реструктурированное SSH меню по категориям
- Пункт полного удаления IPTV Manager
- Все сообщения на русском языке

## Лицензия

MIT
